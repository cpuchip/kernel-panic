# Asset provenance

All art + audio is generated on a local, license-clean pipeline (Dave's asset-harness:
ComfyUI). Deterministic seeds — rerunning a seed reproduces the asset.

## Art — sprites (Z-Image Turbo + ControlNet + BiRefNet, Apache-2.0/MIT chain)

Playful carnival-cartoon direction (Michael's call). One style anchor reused across the
set; crude control primitives (`make_kp_primitives.py`, `make_kp_popcorn.py`) lock the
layout, the prompt does the art. Cropped/resized into `public/assets/sprites/` by
`prep_kp_sprites.py`. The renderer falls back to shapes if a sprite is missing.

**Distinct art per mob (2026-07-07).** All 10 enemies now have their own sprite (was 4
shared). Generated by `scripts/gen-sprites.py` (loops the roster; one carnival STYLE anchor
+ per-mob subject; ControlNet locks the silhouette). Control primitives: the clean `kp_puff`
blob for popped popcorn (poppable/kernel — the lumpy 8-circle primitive traced as a rainbow
flower, so a single-contour blob replaced it), `kp_hard` teardrop (hard/shiney), `kp_cob`
(cob/ton/bcob), `kp_bunch` cluster (bunch), `kp_popcorn` lumpy (bkernel/bpopcorn — the
pattern reads as butter bubbles there).

| File | Seed | What |
|------|------|------|
| `sprites/poppable.png` | 4113 | Poppable Corn Kernel — cream fluffy popcorn puff |
| `sprites/kernel.png` | 4114 | Corn Kernel — golden fluffy popcorn puff |
| `sprites/hard.png` | 4103 | Hard Kernel — amber unpopped teardrop seed |
| `sprites/cob.png` | 4104 | Corn Cob boss — angry corn-on-the-cob (faces right, render rotates to heading) |
| `sprites/bunch.png` | 4105 | Corn Bunch — a cluster of cobs |
| `sprites/ton.png` | 4106 | Corn Ton — armored corn-missile mega-boss |
| `sprites/shiney.png` | 4107 | Shiney Kernel — chrome metallic kernel (laser-proof) |
| `sprites/bkernel.png` | 4108 | Buttery Corn Kernel — glistening butter cluster |
| `sprites/bpopcorn.png` | 4109 | Buttery Popcorn — butter-splashed puff |
| `sprites/bcob.png` | 4110 | Buttery Corn Cob — butter-glazed cob |
| `sprites/fire.png` | 4005 | Fire Tosser (flaming red toaster) — unchanged P1b |
| `sprites/microwave.png` | 4006 | Microwave — unchanged P1b |
| `sprites/laser.png` | 4007 | Laser (ray-gun appliance) — unchanged P1b |
| `sprites/churn.png` | 4008 | Butter Churn — unchanged P1b |
| `sprites/freeze.png` | 4009 | Freeze Ray — icy-blue frost cannon (Tower Update, 2026-07-08) |
| `sprites/butter.png` | 4010 | Butter Turret — golden butter dispenser (Tower Update, 2026-07-08) |
| `sprites/popcorn.png` | 4011 | Popcorn Machine — red-and-white popcorn cart (Tower Update, 2026-07-08) |

Re-roll a mob: `make_kp_primitives.py` for the control image, then
`scripts/gen-sprites.py <name>` (GPU-0 image ComfyUI on :8188), then `prep_kp_sprites.py`.
The old shared `plain/buttered/caramel` sprites remain as fallbacks.

## Audio — SFX

## Audio — SFX

Generated with **Stable Audio 3 Medium Base** — Stability AI Community License (free
commercial < $1M org revenue; we own the outputs; the stack bundles the T5Gemma text
encoder under the Gemma Terms — recorded here per the harness discipline). Post-processing:
loudnorm to −16 LUFS + silence-trim via the harness `optimize_audio.py`. Regenerate/re-roll
any clip with `scripts/gen-sfx.py` (needs the GPU-1 audio ComfyUI instance on :8189).

| File | Seed | Event |
|------|------|-------|
| `sfx/pop.ogg` | 3101 | a kernel pops |
| `sfx/bosspop.ogg` | 3102 | a cob bursts |
| `sfx/fire.ogg` | 3103 | fire tosser shoots |
| `sfx/laser.ogg` | 3104 | laser fires |
| `sfx/microwave.ogg` | 3105 | microwave pulse |
| `sfx/leak.ogg` | 3106 | a kernel reaches the bowl (life lost) |
| `sfx/roundclear.ogg` | 3107 | round cleared / campaign cleared |
| `sfx/bossin.ogg` | 3108 | a boss round begins |
| `sfx/place.ogg` | 3109 | tower placed / upgraded |
| `sfx/sell.ogg` | 3110 | tower sold |
| `sfx/start.ogg` | 3111 | round started |
| `sfx/jackpot.ogg` | 3112 | a buttery mob pops (the big payout) — added 2026-07-07 |
| `sfx/upgrade.ogg` | 3113 | a tower upgrade is bought — added 2026-07-07 |

Sound is user-controllable: the ⚙ settings popover toggles **sound effects** (and
**auto-start next round**), persisted per browser (`kp-sfx`, `kp-autostart`).
